context:
  version: "5.0.0"

recipe:
  name: human-dynamics-estimation-split
  version: ${{ version }}

source:
  - url: https://github.com/robotology/human-dynamics-estimation/archive/refs/tags/v${{ version }}.tar.gz
    sha256: 9185c9fb53f69cd4f797ede5a0fc1a188dcc4e6d6c9341dab84520fc24c851a9

build:
  number: 0

outputs:
  - package:
      name: libhuman-dynamics-estimation
    build:
      script:
        - if: unix
          then: build_cxx.sh
        - if: win
          then: bld_cxx.bat
    requirements:
      build:
        - ${{ compiler('c') }}
        - ${{ stdlib('c') }}
        - ${{ compiler('cxx') }}
        - cmake
        - pkg-config
        - ninja
        - multisheller
        - if: build_platform != target_platform
          then:
            - libyarp
      host:
        - ycm-cmake-modules
        - libyarp
        - libicub-main
        - libtrintrin
        - idyntree
        - libosqp
        - osqp-eigen
        - librobometry
        - libmatio-cpp
        - eigen
        - eigen-abi-devel
      run_exports:
        - ${{ pin_subpackage('libhuman-dynamics-estimation', upper_bound='x.x.x') }}
    tests:
      - script:
          - cmake-package-check IWear --targets IWear::IWear
        requirements:
          run:
            - cmake-package-check
            - ${{ compiler('c') }}
            - ${{ compiler('cxx') }}
            - cmake

  - package:
      name: human-dynamics-estimation
    requirements:
      run_exports:
        - ${{ pin_subpackage('libhuman-dynamics-estimation', upper_bound='x.x.x') }}
      run:
        - ${{ pin_subpackage('libhuman-dynamics-estimation', exact=True) }}
    tests:
      - script:
          - cmake-package-check IWear --targets IWear::IWear
        requirements:
          run:
            - cmake-package-check
            - ${{ compiler('c') }}
            - ${{ compiler('cxx') }}
            - cmake

about:
  homepage: https://github.com/robotology/human-dynamics-estimation
  license: BSD-3-Clause
  license_file: LICENSE
  summary: Software repository for estimating human dynamics.

extra:
  feedstock-name: human-dynamics-estimation
  recipe-maintainers:
    - traversaro
